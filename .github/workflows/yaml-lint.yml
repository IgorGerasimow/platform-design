name: YAML Lint

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  yamllint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install yamllint
        run: pip install yamllint
      - name: Lint Helm charts
        run: |
          find . -name Chart.yaml -exec dirname {} ';' | xargs yamllint
